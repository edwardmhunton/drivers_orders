import React from "react";
import { shallow } from "enzyme";

describe("the fist test", () => {
  it("Should Pass", () => {
    expect(2 + 4).toBe(6);
  });
  it("Should Pass", () => {
    expect(2 + 6).toBe(8);
  });
});
